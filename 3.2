#include<stdio.h>

int main()
{
  int size;
  scanf("%d", &size);
  int arr[size];
  for(int i = 0; i < size; i++)
  {
    scanf("%d", &arr[i]);
  }
  for(int i = 1;i < size; i++)
  {
    int check = 1;
    for(int j = size - 1; j > i; j--)
    {
      if(arr[j] < arr[j - 1])
      {
        int variable_to_exchange =arr[j];
        arr[j] = arr[j -1];
        arr[j - 1] = variable_to_exchange;
        check = 0;
      }
    }
    if(check == 1)
    {
      i = size;
    }
  }

  return 0;
}
